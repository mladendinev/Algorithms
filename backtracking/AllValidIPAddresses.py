def restoreIpAddresses(s):
    """
    :type s: str
    :rtype: List[str]
    """

    # restoreIpAddressesUtil(s, len(s), "")


# def restoreIpAddressesUtil(str, n, acc):
#     isValid(str):
#
#
# def isValid(ipText):
#     addresses = ipText.split(".")
#     firstCond = lambda x: 0 <= x <= 250
#     second  = lambda x: len(x) > 1 and int(x) == 0
#     this
#     return all( firstCond(x) and for x in addresses)
